const apiHost = 'http://localhost:8080/api/'
const filesPath = apiHost + 'files/'
const imagesPath = filesPath + 'images/'

export const environment = {
    production: false,
    apiHost: apiHost,
    filesPath: filesPath,
    imagesPath: imagesPath
  };
  